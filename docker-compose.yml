services:
  bot:
    build:
      context: ./bot
      dockerfile: Dockerfile
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - C:\Users\laiag\Desktop\Christian\jellygram\appdata/bot:/data
  
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    env_file:
      - .env
    volumes:
      - C:\Users\laiag\Desktop\Christian\jellygram\appdata/backend:/data
    ports:
      - "8000:8000"
    restart: unless-stopped
