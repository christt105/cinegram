services:
  # bot:
  #   build:
  #     context: ./bot
  #     dockerfile: Dockerfile
  #   env_file:
  #     - .env
  #   restart: unless-stopped
  #   volumes:
  #     - C:\Users\chris\Desktop\Projects\jellygram\appdata/bot:/data
  
  bot-net:
    build:
      context: ./bot-net
      dockerfile: Dockerfile
    container_name: bot-net
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - C:\Users\chris\Desktop\Projects\jellygram\appdata/bot-net:/data
      # Import directories — set IMPORT_MOVIES_DIR / IMPORT_SHOWS_DIR in .env if you change these paths
      - /mnt/disco/70-79_Media/Peliculas:/data/import/movies:ro
      - /mnt/disco/70-79_Media/Series:/data/import/shows:ro
  
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    env_file:
      - .env
    volumes:
      - C:\Users\chris\Desktop\Projects\jellygram\appdata/backend:/data
    ports:
      - "8000:8000"
    restart: unless-stopped
