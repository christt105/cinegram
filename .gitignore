.env
__pycache__/

# Local compose overrides (host-specific paths)
docker-compose.override.yml

# Core dumps
core.*

# Design sources (not tracked; derived assets live in web/public)
stitch_jellygram_media_manager.zip
icon.png